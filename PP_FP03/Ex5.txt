     double conversao=0.0, valor=0.0, dolar = 1.3, euro = 1.0;
        valor = Integer.parseInt(args[0]);
        
        if ("e".equals(args[1])) {
            conversao = (float) (valor * dolar);
            System.out.println(conversao);
        } else {
            conversao = (float) (valor * euro);
            System.out.println(conversao);
        }
    }